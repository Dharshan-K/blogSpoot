{
  "dependencies": {
    "axios": "^1.4.0",
    "bcryptjs": "^2.4.3",
    "concurrently": "^8.1.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^5.3.0",
    "mongoose": "^7.0.5",
    "nodemon": "^2.0.20",
    "react-router-dom": "^6.11.2"
  },
  "scripts": {
    "server": "nodemon server.js",
    "client":"npm start --prefix frontend",
    "dev":"concurrently \"npm run server\" \"npm run client\"",
    "run-build":"npm install --prefix frontend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "dotenv": "^16.0.3"
  },
    "engines": {
    "node": ">=14.20.1"
  }
}
